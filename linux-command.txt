curl -fsSL https://get.docker.com | sudo sh
    curl	URL에서 데이터를 다운로드하는 명령어
    -f	서버 에러 시 조용히 실패 (HTML 에러 페이지 안 받음)
    -s	진행률 표시 안 함 (silent)
    -S	단, 에러가 나면 에러 메시지는 표시
    -L	리다이렉트 따라감 (URL이 다른 곳으로 이동해도 추적)
|	|  왼쪽 출력을 오른쪽 입력으로 전달
    sudo sh	관리자 권한으로 쉘 스크립트 실행